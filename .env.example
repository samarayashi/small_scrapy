# 環境設置
NODE_ENV=development

# PostgreSQL 配置
POSTGRES_DB=dbname
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# 資料庫連接字串 (用於應用程式)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# 爬蟲配置
CRAWLER_INTERVAL=3600  # 秒數
USER_AGENT_FILE=./resources/user_agents.txt
PROXY_ENABLED=False

# LLM配置（如果需要）
# API_KEY=your_api_key_here
# LLM_API_ENDPOINT=https://api.llm-service.com/v1
# LLM_API_KEY=your_llm_key

# 說明：
# 1. PostgreSQL 配置部分同時用於 docker-compose.yml 和應用程式
# 2. DATABASE_URL 會自動使用上面的 PostgreSQL 配置生成
# 3. 在開發環境中，POSTGRES_HOST 使用 localhost
# 4. 在 Docker 環境中，POSTGRES_HOST 應該使用 service 名稱 (例如: db) 